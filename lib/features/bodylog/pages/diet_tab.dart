import 'package:flutter/material.dart';

class DietTab extends StatelessWidget {
  const DietTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('여기에 식단 타임라인 만들기'));
  }
}